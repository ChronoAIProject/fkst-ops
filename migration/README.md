# Cross-seam duplication ledger

`cross-seam-duplication.allowlist` is shrink-only. Entries may be removed, but
must never be added without the same constraint-based justification and a
concrete root-cause resolution.
